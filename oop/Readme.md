 # Object Oriented Programming (Notes)
  
  ## Meaning
  Simply a better way to write the code ,most importantly when working at the production (organization) lvl - how we deal with real life scenarios in the coding world , (the organized way). This Concepts exists in all the languages ex:-python,Java
  
  *Important terms to now in oop->*
  
**objects** :- objects are entities in the real world

**classes** :- class is like a blueprint of these entities for ex: toyota set up a factory and make blueprint there that how its cars should look , so it sees the blueprint, and that blueprint is  called class .

ex2 :- if i want to store a data of teachers of my school so what are the things that are necessary to be filled . those things are going to define my blueprint 

**teacher(entity)-->name,departement,subject,salary[ a taxcalculating funtion that we need to associate with the teacher so we need to add a function like tax()-->(this is the function that is associated with the teacher which is called methods) ],age(properties)**

  
 